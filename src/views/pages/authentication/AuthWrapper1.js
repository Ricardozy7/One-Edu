// material-ui
import { styled } from '@mui/material/styles';


const AuthWrapper1 = styled('div')(({ theme }) => ({
    backgroundColor: '#15133f',
    overflowX: 'hidden',
    zIndex: 3000
}));

export default AuthWrapper1;
